<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Lista de Videojuegos</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-image: url('https://i.etsystatic.com/20267346/r/il/0259f9/2813877873/il_fullxfull.2813877873_8v0w.jpg');

        }

        .container {
            width: 90%;
            max-width: 1000px;
            margin: 40px auto;
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            padding: 40px;
            border-radius: 8px;
        }

        h1 {
            font-size: 36px;
            margin-bottom: 30px;
            color: #ffffff;
            text-align: center;
            
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .card {
            flex: 0 0 calc(33.333% - 20px);
            margin-bottom: 20px;
            border-radius: 8px;
            background-color: #FFFFFF;
            overflow: hidden;
            box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: scale(1.03);
        }

        .card-img-top {
            width: 100%;
            height: 390px;
            object-fit: cover;
        }

        .card-body {
            padding: 15px;
        }

        .card-title {
            font-size: 20px;
            margin-bottom: 8px;
        }

        .card-text {
            font-size: 16px;
            margin-bottom: 4px;
        }

        .btn-primary {
            padding: 8px 16px;
            margin-top: 20px;
            font-size: 14px;
            font-weight: 500;
            background-color: #128714;
            color: #FFFFFF;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #0c5a0e;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Lista de Videojuegos</h1>
    <br>
    <div class="row">
        <% videojuegos.forEach(videojuego => { %>
            <div class="card">
                <% if(videojuego.imagen) { %>
                    <img src="<%= videojuego.imagen %>" alt="<%= videojuego.titulo %>" class="card-img-top">
                <% } else { %>
                    <div class="text-center p-3">
                        <p class="mb-0">No disponible</p>
                    </div>
                <% } %>
                <div class="card-body">
                    <h5 class="card-title"><%= videojuego.titulo %></h5>
                    <p class="card-text">Plataforma: <%= videojuego.plataforma %></p>
                    <p class="card-text">Género: <%= videojuego.genero %></p>
                    <p class="card-text">Precio: $ <%= videojuego.precio.toFixed(2) %></p>
                </div>
            </div>
        <% }); %>
    </div>

    <div class="text-center mt-4">
        <button onclick="window.location.href='/'" class="btn btn-primary">Regresar al inicio</button>
    </div>
</div>

</body>
</html>
